<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="project">
<insert id="insertMember" parameterType="com.yj.project.member.model.vo.Member">
insert into member values(#{member_id},#{member_pw},#{member_name},#{member_email},#{member_birth},#{university},#{member_addr},#{member_level},#{member_phone},sysdate,#{enroll_category,typeHandler=StringArray})

</insert>
<resultMap type="com.yj.project.member.model.vo.Member" id="selectMap">
<result column="hobby" property="hobby" typeHandler="com.yj.project.common.StringArrayTypeHandler"/>
</resultMap>
<select id="selectOne" parameterType="string" resultMap="selectMap">
select * from member where member_id=#{member_id}
</select>



<select id="selectCheck" parameterType="string" resultType="_int"> select count(*) from member where member_id=#{member_id} </select>

</mapper>
